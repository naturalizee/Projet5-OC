import React from 'react'
import { LayoutHeader, LayoutFooter } from '../Components/Layout';

export function About() {
    return <div>
        < LayoutHeader />
        <div>Page de A propos</div>
        < LayoutFooter />
    </div>
}